{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ads list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Image</th>
                <th>Owner</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ad in ads %}
            <tr>
                <td><a href="{{ path('ads_show', { 'id': ad.id }) }}">{{ ad.id }}</a></td>
                <td>{{ ad.name }}</td>
                <td>{{ ad.description }}</td>
                <td>{{ ad.image }}</td>
                <td>{{ ad.owner }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('ads_show', { 'id': ad.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('ads_edit', { 'id': ad.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ads_new') }}">Create a new ad</a>
        </li>
    </ul>
{% endblock %}
